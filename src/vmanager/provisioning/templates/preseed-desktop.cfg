# Desktop Ubuntu Preseed Configuration
# Generated for VirtUI Manager - Desktop Installation

# Locale and keyboard
d-i debian-installer/locale string {locale}
d-i keyboard-configuration/xkb-keymap select {keyboard}

# Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string {hostname}
d-i netcfg/get_domain string localdomain

# Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string archive.ubuntu.com
d-i mirror/http/directory string /ubuntu

# User setup
d-i passwd/user-fullname string {username}
d-i passwd/username string {username}
d-i passwd/user-password password {password}
d-i passwd/user-password-again password {password}
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false

# Clock and time zone
d-i clock-setup/utc boolean true
d-i time/zone string {timezone}
d-i clock-setup/ntp boolean true

# Disk partitioning with more space for desktop
d-i partman-auto/disk string {disk_device}
d-i partman-auto/method string lvm
d-i partman-auto/choose_recipe select atomic
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Package selection - Desktop environment
tasksel tasksel/first multiselect ubuntu-desktop
d-i pkgsel/include string openssh-server curl wget git vim firefox chromium-browser \
    code htop tree build-essential python3-pip nodejs npm \
    libreoffice gimp vlc thunderbird
d-i pkgsel/upgrade select safe-upgrade

# Boot loader
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string {disk_device}

# X11 and desktop setup
xserver-xorg xserver-xorg/autodetect_monitor boolean true
xserver-xorg xserver-xorg/config/monitor/selection-method select medium
xserver-xorg xserver-xorg/config/monitor/mode-list select 1024x768 @ 60 Hz

# Finish installation
d-i finish-install/reboot_in_progress note

# Post-installation commands for desktop
d-i preseed/late_command string \
    in-target systemctl enable ssh; \
    in-target systemctl enable gdm3; \
    in-target systemctl set-default graphical.target; \
    in-target echo "Desktop preseed installation completed" > /var/log/preseed.log